#include <stdio.h>

int main() {
    /*int n, i, j, sum;

    // Kullanýcýdan giriþ al
    printf("Bir sayi girin: ");
    scanf("%d", &n);

    printf("1'den %d'ye kadar olan mukemmel sayilar:\n", n);

    // 1'den n'ye kadar olan sayýlarý kontrol et
    for (i = 1; i <= n; i++) {
        sum = 0;

        // i'nin bölenlerini bul ve toplamlarýný hesapla
        for (j = 1; j < i; j++) {
            if (i % j == 0) {
                sum += j;
            }
        }

        // Eðer bölenlerin toplamý i'ye eþitse, i mükemmel sayýdýr
        if (sum == i) {
            printf("%d\n", i);
        }
    }

    return 0;*/
//-------------------------------------------------------------------------------------------soru------------------------------------------------------------
//bir pozitif bir negatif toplama
/*int a=1;
double sum=0,x=0,y=0;
while(a<=100){
    if(a%2==0){
        x+=1.0/a;
    }else
    y+=1.0/a;
}
a++;
sum=y-x;
 printf("Sonuç: %f\n", sum);*/




    /*double sum = 0, x = 0, y = 0;
    int a = 1;

    while (a <= 100) {
        if (a % 2 == 0) {
            x += 1.0 / a;
        } else {
            y += 1.0 / a;
        }
        a++; // Döngü değişkenini artır
    }

    sum = y - x;
    printf("Sonuç: %f\n", sum);

    return 0;*/

//.....................................................................soruuu............................................................
   /* int st=1;
        do{
        printf("%d\n",st+2);
        st=st*(-2);
    }
        while(st>=-18);*/

//..........................................................................................................................

/*int i,j,k;
for(j=1;j<=6;j++){
    for(i=6;i>=j;i--){
        printf(" ");
    }
    for(k=1;k<=3;k++){
        printf("*");
    }
printf("\n");
}*/





}
